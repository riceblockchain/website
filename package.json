{
  "name": "rice-blockchain",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export -o docs",
    "deploy": "next build && next export -o docs && cd docs && touch CNAME && echo \"blockchain.rice.edu\" >> CNAME && touch .nojekyll && cd .. && git add docs && git commit -m \"deploy new build\""
  },
  "dependencies": {
    "es6-promise": "^4.2.5",
    "front-matter": "^3.0.1",
    "fs": "^0.0.1-security",
    "next": "^7.0.2",
    "next-images": "^1.0.4",
    "react": "^16.7.0",
    "react-dom": "^16.7.0"
  }
}
